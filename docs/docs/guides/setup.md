# Setting Up Agrigate

## Server

## Client

:::info

When connecting to an EMQX MQTT broker over websockets, the `Host` setting must
be prefixed with `wss://` suffixed with `/mqtt`. For example:
`wss://my.emqx.host.com/mqtt`

:::
